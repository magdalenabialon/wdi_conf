<div class="home">
    <div class="container">
        <div class="login-page clearfix">
          <div class="logged-in-box auth0-box logged-in">
            <h1 id="logo"><img src="//cdn.auth0.com/samples/auth0_logo_final_blue_RGB.png" /></h1>

            <h2>Hi, <%= @user.name %></h2>
            <h4> <%= @user.email %> </h4>

            <img src="<%= @user.image_url %> " alt="user_avatar">
            <!-- <%= @user.inspect%> -->
          </div>
        </div>
    </div>
</div>
